# T034 · Детерминизм турниров (server-fixed seed)

> Сгенерировано из `_meta/tasks.json`. Не редактировать вручную.

- **Фаза:** 3 — Движок сценариев (scenario-gen + seedrandom)
- **Трек:** `shared`
- **Статус:** `todo`
- **Исполнитель:** —
- **Зависимости:** T032, T100
- **CEO-гейт:** нет
- **Обновлено:** 2026-09-08

## Цель
Сервер фиксирует seed турнира; клиент воспроизводит идентично; защита от утечки future.

## Критерии приёмки
- [ ] Все участники получают один dataset + одну точку раскрытия
- [ ] future не утекает до решения (server-side hash)

## Тест-план
- Vitest: серверный seed → клиентский replay идентичен

## Обязательные ограничения
- Следовать `github_project/02_TECH_STACK.md` и `github_project/00_CONVENTIONS.md`.
- Не подменять компонент стека молча. Сначала диагностировать и зафиксировать доказательство; затем пометить задачу `blocked`.
- Отсутствующий необязательный арт заменять manifest-заглушкой `placeholder: true`, не выдумывая финальный ассет.
- Не использовать React в игровом UI; не кодировать смысл только цветом.

> 📌 Блокируется готовностью сервера (T100).

## Лог статуса
| Дата | Агент | Статус | Заметка |
|---|---|---|---|
| 2026-09-08 | init | `todo` | задача импортирована в единый реестр |

## Команды статуса
```bash
python3 github_project/_meta/generate.py set-status T034 in_progress --agent <id> --note "что начато"
python3 github_project/_meta/generate.py set-status T034 done --agent <id> --note "тесты и результат"
```
