# T005 · Финализация github_project (эта система)

> Сгенерировано из `_meta/tasks.json`. Не редактировать вручную.

- **Фаза:** 0 — Фундамент и конвенции (Foundation)
- **Трек:** `qa`
- **Статус:** `todo`
- **Исполнитель:** —
- **Зависимости:** T000
- **CEO-гейт:** нет
- **Обновлено:** 2026-09-08

## Цель
Довести папку github_project до читаемого на GitHub состояния: README, INDEX, CONVENTIONS, PRODUCT, TECH_STACK, MASTER_PLAN, STATUS_LOG, AGENT_PROTOCOL, REVIEW_WORKFLOW, TRACKS, assets/*, admin/*, ceo/*.

## Критерии приёмки
- [ ] Любой агент по ссылке на репо понимает, с чего начать (README→INDEX)
- [ ] Статус-система и правило «нет обходного пути при недоступном стеке» задокументированы
- [ ] STATUS_LOG и tasks сгенерированы скриптом

## Тест-план
- grep: каждая задача из STATUS_LOG имеет файл tasks/

## Обязательные ограничения
- Следовать `github_project/02_TECH_STACK.md` и `github_project/00_CONVENTIONS.md`.
- Не подменять компонент стека молча. Сначала диагностировать и зафиксировать доказательство; затем пометить задачу `blocked`.
- Отсутствующий необязательный арт заменять manifest-заглушкой `placeholder: true`, не выдумывая финальный ассет.
- Не использовать React в игровом UI; не кодировать смысл только цветом.

> 📌 Эта задача выполняется оркестратором при инициализации; далее поддерживается агентами.

## Лог статуса
| Дата | Агент | Статус | Заметка |
|---|---|---|---|
| 2026-09-08 | init | `todo` | задача импортирована в единый реестр |

## Команды статуса
```bash
python3 github_project/_meta/generate.py set-status T005 in_progress --agent <id> --note "что начато"
python3 github_project/_meta/generate.py set-status T005 done --agent <id> --note "тесты и результат"
```
