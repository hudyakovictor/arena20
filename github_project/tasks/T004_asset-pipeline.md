# T004 · Пайплайн сборки ассетов

> Сгенерировано из `_meta/tasks.json`. Не редактировать вручную.

- **Фаза:** 0 — Фундамент и конвенции (Foundation)
- **Трек:** `frontend`
- **Статус:** `todo`
- **Исполнитель:** —
- **Зависимости:** T000, T002, T003
- **CEO-гейт:** нет
- **Обновлено:** 2026-09-08

## Цель
Скрипт сборки: SVG→SVGO→texture-atlas/Phaser-texture; генерация manifest; бюджеты в CI.

## Критерии приёмки
- [ ] Все SVG из signal_arena_skill_icons_svg.zip проходят SVGO (≤400Б/иконку, core≤12КБ)
- [ ] Атлас/текстуры собираются в build; tint по semantic token
- [ ] Растр (webp) лениво грузится по pack; hero-art не попадает в первый сценарий
- [ ] CI проверяет бюджеты веса

## Тест-план
- Vitest: размер атласа ≤лимита
- Playwright: иконки рендерятся с tint

## Обязательные ограничения
- Следовать `github_project/02_TECH_STACK.md` и `github_project/00_CONVENTIONS.md`.
- Не подменять компонент стека молча. Сначала диагностировать и зафиксировать доказательство; затем пометить задачу `blocked`.
- Отсутствующий необязательный арт заменять manifest-заглушкой `placeholder: true`, не выдумывая финальный ассет.
- Не использовать React в игровом UI; не кодировать смысл только цветом.

> 📌 ИКОНКИ: знак на иконке больше плашки под ним — уменьшить знак (см. assets/ICON_NOTES.md). Запрет: emoji/icon-font как prod-иконки, PNG для обычного UI.

## Лог статуса
| Дата | Агент | Статус | Заметка |
|---|---|---|---|
| 2026-09-08 | init | `todo` | задача импортирована в единый реестр |

## Команды статуса
```bash
python3 github_project/_meta/generate.py set-status T004 in_progress --agent <id> --note "что начато"
python3 github_project/_meta/generate.py set-status T004 done --agent <id> --note "тесты и результат"
```
